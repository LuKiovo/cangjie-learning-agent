# LLM Responses

This directory stores local Markdown copies of LLM responses imported through:

```powershell
target\manual\main.exe import-llm-response <name> <file-path>
```

The project does not store API keys. Responses saved here are local learning artifacts for classroom review and follow-up study.
