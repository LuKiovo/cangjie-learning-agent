# Cangjie Study Card: ArrayList

## Source

- Match count: 2
- Primary source: F:\学校の\小学期\make_agent\knowledge_base/official\course_collection_note.md

## Beginner Explanation

Read the primary source first, then explain `ArrayList` with one concrete Cangjie syntax rule.

## Source Context

```text
# Cangjie Collection Types

Topic keywords: Cangjie Collection, ArrayList, HashMap, Iterable, Collections utility.

Beginner note:

- Collection types store multiple values and are useful in menu, list, and table-like programs.
- `ArrayList` is useful when values are ordered and the program needs list-style access.
- `HashMap` is useful when values should be found by key.
- Course projects can use collections to store menu items, user choices, and simple records.

Tiny example idea:

```cangjie
main(): Int64 {
    println("Use ArrayList for ordered values.")
    println("Use HashMap for key-value lookup.")
    return 0
}
```

```

## Memory Points

1. Name the syntax point in one short sentence.
2. Write a tiny Cangjie example that uses `ArrayList`.
3. Save your own note with `add-note` or `add-example` after practicing.

## Practice

- Task: write a minimal Cangjie program or expression about `ArrayList`.
- Check: explain which line in the primary source supports your answer.
- Follow-up: run `quiz ArrayList` or `session ArrayList` for review.
