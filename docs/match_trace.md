# Cangjie Learning Agent Trace: match

## Trace Steps

1. Receive user topic or question: `match`.
2. Search official/course knowledge and user notes.
3. Score complete-query matches higher than word-level fallback matches.
4. Select the primary source with the highest local score.
5. Build a source-backed answer, card, session, or LLM request draft.
6. Keep the source path visible for classroom review.

## Retrieval Result

- Total matches: 2
- Best official/course score: 11
- Best user notes score: 0
- Primary source: F:\学校の\小学期\make_agent\knowledge_base/official\course_match_note.md

## Source Context

````text
# Cangjie Match And Control Flow

Topic keywords: Cangjie match expression, if expression, while loop, for in loop, enum.

Beginner note:

- `if` is useful for two-way or multi-way conditions.
- `match` is useful when a value should be compared against several cases.
- Loops such as `while` and `for in` help repeat work.
- A terminal menu can use `match` to dispatch user commands.

Tiny example:

```cangjie
main(): Int64 {
    let command = "help"
    match (command) {
        case "help" => println("show help")
        case "quit" => println("quit")
        case _ => println("unknown command")
    }
    return 0
}
```

````

## Engineering Notes

- The trace is generated by Cangjie code.
- It explains why the selected source is used.
- It supports the project requirement that answers must mark their sources.
